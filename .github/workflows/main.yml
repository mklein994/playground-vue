name: Build and Lint Project
on: [push]
jobs:
  setup:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          path: playground-vue

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: "playground-vue/package.json"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: latest
          cache: "pnpm"
          cache-dependency-path: "playground-vue/pnpm-lock.yaml"

      - name: Install dependencies
        working-directory: playground-vue
        run: pnpm install

      - name: Upload playground-vue
        uses: actions/upload-artifact@v4
        with:
          name: playground-vue
          path: playground-vue

  setup-rust:
    runs-on: ubuntu-latest
    steps:
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: wasm32-unknown-unknown

  build-sunrise:
    needs: [setup-rust]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: "mklein994/sunrise-cli"
          path: sunrise-cli

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            sunrise-cli/target/
          key: ${{ runner.os }}-cargo-sunrise-${{ hashFiles('sunrise-cli/**/Cargo.lock') }}

      - run: wasm-pack build sunrise-cli

      - name: Upload sunrise-cli pkg
        uses: actions/upload-artifact@v4
        with:
          name: sunrise-cli-pkg
          path: sunrise-cli/pkg/

  build-date-diff:
    needs: [setup-rust]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: "mklein994/date-diff"
          path: date-diff

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            date-diff/target/
          key: ${{ runner.os }}-cargo-date-diff-${{ hashFiles('date-diff/**/Cargo.lock') }}

      - run: wasm-pack build date-diff

      - name: Upload date-diff pkg
        uses: actions/upload-artifact@v4
        with:
          name: date-diff-pkg
          path: date-diff/pkg/

  verify:
    needs: [setup]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        task: ["lint", "test-node", "test-coverage", "typecheck"]
        include:
          - task: lint
            command: pnpm run lint:ci
          - task: test-node
            command: pnpm run test:node
            continue-on-error: true
          - task: test-coverage
            command: pnpm run coverage --poolOptions.threads.isolate true
          - task: typecheck
            command: pnpm run typecheck
    steps:
      - name: Download playground-vue
        uses: actions/download-artifact@v4
        with:
          name: playground-vue
          path: playground-vue

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: "playground-vue/package.json"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: latest
          cache: "pnpm"
          cache-dependency-path: "playground-vue/pnpm-lock.yaml"

      - name: Install dependencies
        working-directory: playground-vue
        run: pnpm install

      - name: Download sunrise-cli pkg
        uses: actions/download-artifact@v4
        with:
          name: sunrise-cli-pkg
          path: sunrise-cli/pkg

      - name: Download date-diff pkg
        uses: actions/download-artifact@v4
        with:
          name: date-diff-pkg
          path: date-diff/pkg

      - name: Run ${{ matrix.task }}
        working-directory: playground-vue
        continue-on-error: ${{ matrix.continue-on-error || false }}
        run: ${{ matrix.command }}

      - name: Upload coverage
        if: matrix.task == 'test-coverage'
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: playground-vue/public/coverage/

  prepare-build:
    needs: [verify]
    runs-on: ubuntu-latest
    outputs:
      version_string: ${{ steps.version.outputs.version_string }}
    steps:
      - name: Get Version String
        id: version
        run: |
          VERSION_STRING="${GITHUB_REF_NAME} (${GITHUB_SHA:0:7} $(date -Is))"
          echo "version_string=${VERSION_STRING}" >> "$GITHUB_OUTPUT"

  build:
    needs: [prepare-build, build-sunrise, build-date-diff]
    runs-on: ubuntu-latest
    steps:
      - name: Download playground-vue
        uses: actions/download-artifact@v4
        with:
          name: playground-vue
          path: playground-vue

      - name: Download coverage
        uses: actions/download-artifact@v4
        with:
          name: coverage
          path: playground-vue/public/coverage

      - name: Download sunrise-cli pkg
        uses: actions/download-artifact@v4
        with:
          name: sunrise-cli-pkg
          path: sunrise-cli/pkg

      - name: Download date-diff pkg
        uses: actions/download-artifact@v4
        with:
          name: date-diff-pkg
          path: date-diff/pkg

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: "playground-vue/package.json"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: latest
          cache: "pnpm"
          cache-dependency-path: "playground-vue/pnpm-lock.yaml"

      - name: Install dependencies
        working-directory: playground-vue
        run: pnpm install

      - name: Build
        working-directory: playground-vue
        env:
          SENTRY_ORG: matthew-klein
          SENTRY_PROJECT: matthew-klein
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          VITE_SENTRY_RELEASE: ${{ github.sha }}
          VITE_SENTRY_ENABLED: true
          BUILDTIME_SENTRY_ENABLED: true
          VITE_COMMIT_HASH: ${{ github.sha }}
          VITE_VERSION_STRING: ${{ needs.prepare-build.outputs.version_string }}
          VITE_SENTRY_DSN: https://931d497682d84de1ad9db23083a1a6c0@o1124115.ingest.sentry.io/6162360
          VITE_SENTRY_ENVIRONMENT: ${{ github.ref_name == 'main' && 'release' || 'development' }}
        run: pnpm run build:all

      - name: Upload build
        uses: actions/upload-artifact@v4
        with:
          name: final-build
          path: playground-vue/dist/

  deploy:
    needs: [build]
    runs-on: ubuntu-latest
    permissions:
      deployments: write
    steps:
      - name: Download final build
        uses: actions/download-artifact@v4
        with:
          name: final-build
          path: playground-vue/dist/

      - name: Deploy to Cloudflare
        working-directory: playground-vue
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: pnpm dlx wrangler pages deploy dist --project-name=playground-vue --branch="${{ github.ref_name }}"
